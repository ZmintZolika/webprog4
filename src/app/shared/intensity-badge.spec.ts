import { IntensityBadge } from './intensity-badge';

describe('IntensityBadge', () => {
  it('should create an instance', () => {
    const directive = new IntensityBadge();
    expect(directive).toBeTruthy();
  });
});
